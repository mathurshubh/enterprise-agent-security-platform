[pytest]
pythonpath = .
testpaths = tests
markers =
    security_baseline: reproduces a weakness confirmed by the post-v0.16 adversarial review
    security_invariant: states a post-hardening security contract (xfail strict until the fix lands)
    security_regression: protects a security control that is already correct today